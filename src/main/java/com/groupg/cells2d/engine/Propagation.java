package com.groupg.cells2d.engine;

/**
 * paramètres du la fonction de l'évolution de propagation à voir pour que ce ne soit pas la même
 * chose que simulationparams et/ou seircalculator
 */

public class Propagation {

    // deux constructeur un standard l'autre custom, on va utiliser la surcharge (overload)
    // bon point java je trouve plus ça nous réduit le nombre de fichiers et en plus les deux faisaient la même chose
}
